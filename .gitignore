node_modules/
dist/
.vite/
coverage/

# Python venv (per-package, used only by extraction scripts)
**/.venv/
**/__pycache__/
*.pyc

*.log
.DS_Store

.env
.env.*
!.env.example

# Source PDFs (re-distribution license undecided; keep local only)
packages/*/data/source/*.pdf

# Geocoded data file is committed (it's the dataset), but not the cache —
# cache is ephemeral and regenerable from raw + Kakao API.
# (Decision: commit cache too — saves API quota on rebuilds. Override here if needed.)

